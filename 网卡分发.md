# 校园 VPN 强制全局代理时的临时分流教程（macOS）

> 适用场景：连接校园 VPN 后，学校内网服务器可以访问，但 Codex、OpenAI、GitHub 或普通网页无法正常使用。  
> 核心思路：让“校园内网地址”继续走校园 VPN，把“默认上网流量”改回本地网络或你常用的代理。

本文以 macOS 终端为例。Windows / Linux 的命令不同，但思路类似：先确认 VPN 接管了哪条路由，再给校园内网地址单独加路由，最后恢复默认路由。

## 先理解几个变量

下面这些值不要直接照抄，需要根据自己电脑的实际输出替换。

```text
校园服务器地址：CAMPUS_IP
本地网关地址：LOCAL_GATEWAY
校园 VPN 网关：VPN_GATEWAY
校园 VPN 网卡：VPN_INTERFACE
本地上网网卡：LOCAL_INTERFACE
```

常见例子：

```text
CAMPUS_IP       = 172.25.25.189
LOCAL_GATEWAY   = 192.168.101.1
VPN_GATEWAY     = 1.1.1.1
VPN_INTERFACE   = utun8
LOCAL_INTERFACE = en0
```

注意：`1.1.1.1`、`utun8`、`en0` 都不是固定值。不同学校 VPN、不同电脑、不同连接顺序下都可能变化。

## 判断是不是全局 VPN

连接校园 VPN 后执行：

```bash
route -n get default
```

如果看到类似结果：

```text
destination: default
gateway: 1.1.1.1
interface: utun8
```

通常说明默认 IPv4 流量已经被校园 VPN 接管，也就是“全局 VPN”或“全隧道”模式。

但这不是所有校园 VPN 的固定输出。你需要记录自己的：

```text
VPN_GATEWAY   = gateway 后面的值
VPN_INTERFACE = interface 后面的值
```

如果 `interface` 是 `utun` 开头的网卡，例如 `utun3`、`utun5`、`utun8`，一般就是 VPN 隧道接口。

## 查询本地网关

建议在连接校园 VPN 之前查询一次本地网关。

如果你用 Wi-Fi，通常先试：

```bash
ipconfig getoption en0 router
```

如果有输出，例如：

```text
192.168.101.1
```

记下来：

```text
LOCAL_GATEWAY = 192.168.101.1
```

如果 `en0` 没有输出，先查看实际网卡名：

```bash
networksetup -listallhardwareports
```

找到 Wi-Fi 或有线网卡对应的 `Device`，例如 `en1`、`en2`，再查询：

```bash
ipconfig getoption en1 router
```

## 临时分流操作

目标：

```text
校园服务器 / 校园内网地址 -> 走校园 VPN
Codex / OpenAI / GitHub / 普通网页 -> 走本地网络或普通代理
```

操作顺序：

1. 关闭所有 VPN 和代理。

2. 查询并记录本地网关。

   ```bash
   ipconfig getoption en0 router
   ```

   如果你的本地网卡不是 `en0`，把命令里的 `en0` 换成实际网卡名。

3. 连接校园 VPN。

4. 查询并记录校园 VPN 的默认路由。

   ```bash
   route -n get default
   ```

   记录其中的 `gateway` 和 `interface`。

5. 确认校园服务器能访问。

   ```bash
   ping CAMPUS_IP
   ```

   或：

   ```bash
   ssh 用户名@CAMPUS_IP
   ```

6. 给校园服务器添加单独路由，让它继续走校园 VPN。

   ```bash
   sudo route add -host CAMPUS_IP VPN_GATEWAY
   ```

   例如：

   ```bash
   sudo route add -host 172.25.25.189 1.1.1.1
   ```

7. 把默认 IPv4 路由改回本地网关。

   ```bash
   sudo route change default LOCAL_GATEWAY
   ```

   例如：

   ```bash
   sudo route change default 192.168.101.1
   ```

8. 再打开 Codex 使用的 VPN 或代理。

9. 检查分流是否成功。

   ```bash
   route -n get CAMPUS_IP
   route -n get 8.8.8.8
   ```

理想结果：

```text
CAMPUS_IP -> interface: utunX
8.8.8.8  -> interface: enX
```

其中 `utunX` 是校园 VPN 网卡，`enX` 是本地 Wi-Fi 或有线网卡。

## 常用检查命令

查看默认路由：

```bash
route -n get default
```

或：

```bash
netstat -rn | grep default
```

查看某个地址实际走哪张网卡：

```bash
route -n get 8.8.8.8
route -n get CAMPUS_IP
```

查看所有网络服务：

```bash
networksetup -listallhardwareports
```

查看某张网卡的本地网关：

```bash
ipconfig getoption en0 router
```

## 恢复方法

这些 `route` 命令通常只影响当前网络会话，不是永久配置。断开 VPN、重连网络或重启后一般会恢复。

如果改完路由后网络异常，可以按这个顺序恢复：

1. 断开校园 VPN。

2. 关闭 Codex 使用的 VPN 或代理。

3. 关闭并重新打开 Wi-Fi。

4. 重新连接网络后检查默认路由。

   ```bash
   route -n get default
   ```

5. 必要时重启电脑。

如果只是想删除刚才添加的单独主机路由，可以执行：

```bash
sudo route delete -host CAMPUS_IP
```

例如：

```bash
sudo route delete -host 172.25.25.189
```

## 常见问题

### `gateway: 1.1.1.1` 是固定的吗？

不是。它只是某些 VPN 客户端给出的隧道网关显示。你的电脑上可能是其他地址，也可能显示为 `link#...`。

### `interface: utun8` 是固定的吗？

不是。`utun` 编号由 macOS 动态分配，可能每次连接都不一样。判断时看 `route -n get default` 的实际输出。

### 为什么我改完默认路由后又变回 VPN？

有些校园 VPN 会持续监控并强制恢复全局路由。这种情况下，临时分流可能不稳定。可以重新执行分流命令，不过我目前测试非常稳定

### 已经 SSH 到服务器了，能不能断开校园 VPN？

如果服务器只能通过校园 VPN 访问，断开 VPN 后 SSH 路径会消失，连接通常会卡住或超时。

长期任务建议在服务器上使用 nohup 或者 tmux 在服务器后台运行：


